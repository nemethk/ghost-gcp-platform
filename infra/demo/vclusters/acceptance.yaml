# vcluster values for the Acceptance environment — one worker node (see
# test.yaml for why).
#
# Deploy with:
#   vcluster use driver docker
#   vcluster create acceptance --driver docker --values acceptance.yaml

networking:
  podCIDR: 10.64.0.0/16
  serviceCIDR: 10.128.0.0/16

controlPlane:
  distro:
    k8s:
      enabled: true
      image:
        tag: v1.36.0
  statefulSet:
    resources:
      requests:
        cpu: 200m
        memory: 256Mi
      limits:
        cpu: 1
        memory: 1Gi

experimental:
  docker:
    nodes:
      - name: worker-1
    ports:
      - "8080:80"
      - "8443:443"

sync:
  fromHost:
    nodes:
      enabled: false
