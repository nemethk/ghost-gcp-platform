apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: ghost
spec:
  values:
    ghost:
      url: "https://localhost:8443"
      database:
        existingSecret: ghost-db-credentials
